# AGENTS.md

本仓库按照 **LLM Wiki** 模式组织，目标是在 `raw/` 原始资料层与使用者之间，维护一个由 LLM 持续增量更新的 Obsidian 知识库。

## 1. 三层架构

### 原始资料层（只读）
- 位置：`raw/`
- 作用：存放原始来源文档、截图、图片、会议记录、网页剪藏等。
- 规则：**永不覆盖、永不改写原始内容。** 允许读取、引用、建立链接，但不直接编辑其正文来“整理”。

### Wiki 层（LLM 维护）
- 位置：`wiki/`
- 作用：存放 LLM 生成和维护的摘要、主题页、实体页、比较页、问答沉淀、索引与日志。
- 规则：用户主要阅读这一层，LLM 主要写这一层。

### Schema / 维护规则层
- 文件：`AGENTS.md`
- 作用：规定目录结构、命名、摄取流程、问答沉淀规则、lint 规则与敏感信息处理原则。

## 2. 目录约定

- `wiki/index.md`：内容索引。回答问题前优先读取。
- `wiki/log.md`：按时间追加的操作日志。
- `wiki/00-总览/`：知识库主页、领域地图、导航页。
- `wiki/10-主题/`：主题页、概念页、综述页。
- `wiki/20-实体/`：人物、产品、项目、公司、工具、模块等实体页。
- `wiki/30-来源/`：来源总表、来源摄取计划、来源摘要页。
- `wiki/40-问答沉淀/`：把高价值问答、对比分析、阶段性结论沉淀成页面。
- `wiki/90-模板/`：可复用页面模板。

## 3. 页面类型与命名

推荐使用以下页面类型（frontmatter 中的 `type` 字段）：
- `overview`：总览/导航页
- `topic`：主题页
- `entity`：实体页
- `source-index`：来源目录页
- `source-summary`：单一来源摘要页
- `qa`：问答沉淀页
- `template`：模板页
- `meta`：系统/维护页

命名原则：
1. 页面名要稳定、可复用、可被多次更新；避免一次性临时标题。
2. 优先使用中文名，除非技术名词更适合英文（如 `Claude Code`、`Proxy`）。
3. 领域地图统一命名为 `领域地图-XXX`。
4. 来源摘要页建议命名为 `来源摘要-原文件名`。
5. 问答沉淀页建议命名为 `问答-主题-YYYY-MM-DD`。

## 4. 摄取（Ingest）工作流

当用户要求“处理/摄取一个来源”时，按如下顺序执行：

1. 读取 `wiki/index.md`，了解现有结构。
2. 读取相关领域地图与已有主题页，避免重复造页。
3. 读取 `raw/` 中指定来源。
4. 产出或更新：
   - 一个来源摘要页（如有必要）
   - 相关主题页
   - 相关实体页
   - `wiki/index.md`
   - `wiki/log.md`
5. 如果发现新来源与旧结论冲突：
   - 在相关页面明确记录“旧说法 / 新证据 / 当前判断”
   - 不要静默覆盖争议结论
6. 如果用户提问中产生了高价值结论，可额外写入 `wiki/40-问答沉淀/`

## 5. 问答（Query）工作流

回答知识类问题时：
1. 先查 `wiki/index.md`
2. 再查相关主题页、实体页、来源页
3. 若 wiki 中尚无足够内容，再回到 `raw/` 查原始资料
4. 回答时优先引用 wiki，必要时补充原始来源路径
5. 若输出具有长期价值，主动建议沉淀到 `wiki/40-问答沉淀/`

## 6. Lint / 健康检查规则

定期检查：
- 是否存在孤儿页（没有入链）
- 是否有高频出现但未建页的概念或实体
- 是否存在互相矛盾的结论
- 是否有已过时结论需要被新来源修正
- 是否有“应该在来源摘要中、却散落在别处”的内容
- `wiki/index.md` 是否遗漏新页面
- `wiki/log.md` 是否缺少关键操作记录

## 7. 索引与日志规则

### index
- 按页面类型分区列出所有重要页面
- 每个条目包含：链接 + 一句话说明
- 有新页面时同步更新

### log
- 只追加，不回改历史含义
- 条目格式统一为：`## [YYYY-MM-DD] 动作 | 标题`
- 常见动作：`setup`、`ingest`、`query`、`lint`、`refactor`

## 8. 敏感信息与隐私规则

此仓库包含个人与工作资料，必须遵守：

1. **不要把账号、密码、token、cookie、私密标识符原样抄入 wiki。**
2. 如果来源包含敏感字段：
   - 只在 wiki 中记录“存在此类信息”与“用途/风险/位置”
   - 必要时使用脱敏形式（如 `手机号后四位`、`邮箱前缀`）
3. 工作资料默认视为敏感，尤其是：
   - 业务字段定义
   - 项目排期
   - 内部会议记录
   - 账号与权限信息
4. 对敏感来源优先写主题性总结，不直接做全文转述。

## 9. 写作规则

- 使用 Obsidian 原生 wikilink：`[[页面名]]`
- 对 vault 内页面与原始来源都优先使用 wikilink
- 外部网址使用标准 Markdown 链接
- 结论尽量结构化：摘要、关键点、证据、待验证问题
- 避免把同一段长总结复制到多个页面；应以链接代替重复
- 保持页面可持续更新，而不是一次性“聊天纪要风格”页面

## 10. 当前知识域

当前原始资料共 **81** 篇 Markdown 来源，主要分布在：
- `raw/mine/AI 原生开发工作流实战`
- `raw/mine/前端`
- `raw/mine/生活/个人`
- `raw/mine/生活/可可`
- `raw/mine/生活/旅游`
- `raw/mine/生活/菜谱`
- `raw/工作/七猫`

执行任何新增/整理任务时，应优先复用现有结构，不要任意创建平行目录体系。
